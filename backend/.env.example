# Backend runtime
SERVER_PORT=8085

# Master database credentials
DB_URL=jdbc:mysql://localhost:3306/servesmart_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DB_USER=root
DB_PASSWORD=Ajay@111

# Tenant database connection template (used by tenant provisioning/connection providers)
DB_HOST=localhost
DB_PORT=3306
DB_PARAMS=useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# App settings
APP_TENANT_REQUIRE_HEADER=true
APP_SEED_ENABLED=true
APP_SEED_TENANT_ID=1
APP_BACKEND_BASE_URL=http://localhost:8085
APP_MOCK_OTP=123456

# Seed staff credentials (used only when APP_SEED_ENABLED=true and no staff exists)
APP_SEED_ADMIN_USERNAME=admin
APP_SEED_ADMIN_PASSWORD=change_admin_password
APP_SEED_OWNER_USERNAME=owner@servesmart.local
APP_SEED_OWNER_PASSWORD=change_owner_password
APP_SEED_KITCHEN_USERNAME=kitchen
APP_SEED_KITCHEN_PASSWORD=change_kitchen_password
